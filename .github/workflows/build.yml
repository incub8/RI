name: Build

on:
  push:
    branches:
      - 'feature/**'

  pull_request:
    branches:
      - develop

# TODO add master, hotfix, release, younameit

jobs:
  build:

    runs-on: ubuntu-latest

    container:
      image: pipelines-build-environment:feature_CCCP-37-add-github-support

    steps:
      - name: Check out
        uses: actions/checkout@v2
